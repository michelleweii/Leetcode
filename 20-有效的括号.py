# 思路是使用栈，将对应的字符通过字典映射为数字，只要求数字和为0即可
class Solution(object):
    def isValid(self, s):
        """
        :type s: str
        :rtype: bool
        """

if __name__ == '__main__':
    s = "()[]{}"
    print(Solution().isValid(s))